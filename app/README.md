# App

Angular 19 studio UI. From this folder:

- `npm start` — http://localhost:4200 (proxies `/api` to http://localhost:5180)
- `npm run lint` / `npm run lint:fix` — junolint
- `npm test` — Karma/Jasmine
- `npm run build` — production build
